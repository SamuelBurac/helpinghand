import 'package:flutter/material.dart';

class ReviewListingScr extends StatelessWidget {
  const ReviewListingScr({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}