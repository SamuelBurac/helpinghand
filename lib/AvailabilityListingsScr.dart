import 'package:flutter/material.dart';

class AvailabilityListingsScr extends StatelessWidget {
  const AvailabilityListingsScr({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Availability Listings"),
    );
  }
}
