import 'package:flutter/material.dart';

class ReviewPersonListingScr extends StatelessWidget {
  const ReviewPersonListingScr({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}