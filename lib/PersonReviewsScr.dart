import 'package:flutter/material.dart';

class PersonReviewsScr extends StatelessWidget {
  const PersonReviewsScr({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
